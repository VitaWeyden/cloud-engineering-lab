# PostgreSQL database for Echoo.
# Uses a PersistentVolumeClaim so data survives pod restarts.

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: echoo-db-pvc
  namespace: echoo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: echoo-db
  namespace: echoo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: echoo-db
  template:
    metadata:
      labels:
        app: echoo-db
    spec:
      containers:
        - name: echoo-db
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: ECHOO
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: echoo-secret
                  key: DB_PASSWORD
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: pgdata
          persistentVolumeClaim:
            claimName: echoo-db-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: echoo-db
  namespace: echoo
spec:
  selector:
    app: echoo-db
  ports:
    - port: 5432
      targetPort: 5432